var saldo = 0;

if (saldo > 0) {
        var mensagem = "Saldo Positivo.";
        alert (mensagem);
    } else if (saldo == 0) {
        var mensagem = "Saldo Zero!";
        alert (mensagem);
    }
else {
    var mensagem = "Saldo negativo!";
    alert (mensagem);
}