var saldo = 0;

if (saldo >= 0) {
    if (saldo == 0) {
        var mensagem = "Saldo zero";
        alert (mensagem);
    } else {
        var mensagem = "Saldo positivo!";
        alert (mensagem);
    }
}
else {
    var mensagem = "Saldo negativo!";
    alert (mensagem);
}